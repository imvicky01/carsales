import React from "react";
import Navbar from "../Navbar/Navbar";

const NavbarPage = () => {
  return (
    <div>
      <Navbar /> {/* Include your Navbar component here */}
      {/* Add other content for the new page */}
    </div>
  );
};

export default NavbarPage;
